# Terraform — inquisicao_tech (exemplo fictício)

Este código é **apenas demonstrativo** para o desafio da DIO.
IDs de Organization, Billing e credenciais aqui são **fictícios**.

## Placeholders usados
- org_id: `000000000000`
- billing_account: `AAAAAA-BBBBBB-CCCCCC`
- credentials_file: `admin_inqui_tech.json`
- senha (apenas ilustrativa): `*********`

## ⚠️ Segurança (importante)
- **Nunca** suba `*.json` de service account no GitHub.
- O arquivo `admin_inqui_tech.json` neste repositório deve ser tratado como **placeholder**.

## Como rodar (somente se você substituir pelos dados reais)
```bash
terraform init
terraform plan
terraform apply

